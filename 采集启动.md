
# ==========================================
# 0. 每个终端都要先 source
# ==========================================
source /opt/ros/humble/setup.bash
source ~/Documents/elite_robot_ws/install/setup.bash


# ==========================================
# 终端1: 机械臂驱动
# ==========================================
ros2 launch eli_cs_robot_driver elite_control.launch.py robot_ip:=192.168.1.212 cs_type:=cs66


# ==========================================
# 终端2: RealSense 相机
# ==========================================
source /opt/ros/humble/setup.bash
ros2 launch realsense2_camera rs_launch.py \
  camera_namespace:=camera \
  enable_color:=true \
  enable_depth:=false \
  rgb_camera.color_profile:=640x480x30 \
  align_depth.enable:=false


# ==========================================
# 终端3: ArUco 检测 + TF 发布 (带预览窗口)
# ==========================================
cd ~/Documents/elite_robot_ws/biaoding
python3 aruco_single_tf.py


# ==========================================
# 终端4: 标定服务
# ==========================================
source ~/Documents/elite_robot_ws/install/setup.bash
ros2 launch easy_handeye2 elite_handeye_calibrate.launch.py


# ==========================================
# 操作命令 (任意终端, 需 source 过):
# ==========================================
source ~/Documents/elite_robot_ws/install/setup.bash

# 采集 (移动到位后按回车)
ros2 service call /easy_handeye2/calibration/take_sample easy_handeye2_msgs/srv/TakeSample

# 查看数量
ros2 service call /easy_handeye2/calibration/get_sample_list easy_handeye2_msgs/srv/TakeSample 2>&1 | grep -oP 'Sample\(' | wc -l

# 计算 (20组后)
ros2 service call /easy_handeye2/calibration/compute_calibration easy_handeye2_msgs/srv/ComputeCalibration

# 保存
ros2 service call /easy_handeye2/calibration/save_calibration easy_handeye2_msgs/srv/SaveCalibration
