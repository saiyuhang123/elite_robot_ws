
## 根本原因（三层问题叠加）

**1. 虚拟 TCP 偏移（最隐蔽、最核心的问题）**

控制器上报的 `tool0_controller` 和物理法兰 `cs66_tool0` 之间有一个 ≈118° 的固定旋转（TCP 配置偏移，平移为零）。这导致：

- 所有姿态数据（示教器读数、TCP 上报）都是"虚拟 TCP"的朝向，不是物理法兰的
- 倾斜角算出一堆互相矛盾的数（8.7°、45°、66.85°）
- IK 约束"虚拟 TCP 朝下"，物理法兰却偏到水平

**2. EliRobot 脚本的 movej 只认关节角**

`movej([x,y,z,rx,ry,rz])` 会被当成 6 个关节角弧度值执行，机械臂乱跑（"反向转"）。位姿目标必须先本地 IK。

**3. 抓取链路坐标系混用**

手眼标定用的是 `tool0`（物理法兰），抓取脚本却用虚拟 TCP 的姿态去套标定结果，造成 20 多厘米的系统性位置偏差。

## 所有改动

**`biaoding/visual_grasp_test.py`（抓取主脚本）**

- 运动方式：MoveIt → 控制器原生 movej（`/script_sender/script_command` 发脚本）
- 位姿目标改为**本地 5 维 IK + 关节角 movej**（IK 只约束位置 + 工具轴方向，自转放开就近）
- 坐标链姿态来源：虚拟 TCP → **当前关节角 FK**（物理法兰系，与手眼标定一致）
- 预抓取点：世界系竖直上方 20cm（`V_UP_IN_BASE` 用实测值 `(-0.7431, 0.0120, 0.6691)`，倾斜角 ≈48°）
- FK 验证：位置误差 > 2cm 或方向误差 > 5° 拒绝运动
- 退出时自动恢复驱动外部控制程序

**`src/hello_moveit/scripts/joint_jog_gui.py`（点动 GUI）**

- 从 MoveIt 服务改为控制器原生 movej/movel（script_sender 发 def 脚本）
- 六轴关节点动 + TCP 直线步进，倾斜补偿的 X/Y/Z 全走世界系方向（实测 v_up 构建交基）
- "标定竖直方向"按钮改查物理法兰 TF；新增"恢复驱动外部控制"按钮

**`src/elite_robot_example/.../robot_cartesian_control.py`**

- 新增 `cs66_inverse_kinematics_5()`：5 维数值 IK（位置 + 工具轴方向，支持自定义工具轴）

**`biaoding/test_ik.py`（新增）**

- 独立 IK 测试工具：手动输目标点验证解算，`!` 后缀直接执行，与视觉链路完全解耦

其他**

- `start_grasp.sh`：去掉 move_group 和 grasp_move_server（3 步启动）
- `启动说明.md`：同步新流程和注意事项
- `grasp_moveit_msgs/srv/MoveToPose.srv` + `move_to_pose_server.cpp`：加过 motion_type/MoveL 分支（现在不在这条链路使用）

。